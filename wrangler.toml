# Must match the existing Cloudflare Worker, not the package name (a11y-quest).
name = "a11yquest"
main = "worker/index.ts"
compatibility_date = "2026-06-22"

[assets]
directory = "./dist"
binding = "ASSETS"
run_worker_first = ["/", "/index.html"]
